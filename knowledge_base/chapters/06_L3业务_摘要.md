# 06 L3业务

## 章节概述



## 命令数量

- 子章节数：4
- 命令总数：

## 子章节列表

### BFD配置命令 (104个命令)

- $(authentication-active)
- $(authentication-active)
- $(authentication-active)
- $(authentication-active)
- $(authentication-active)
- $(authentication-active)
- $(authentication)
- $(authentication)
- $(authentication)
- $(authentication)
- $(authentication)
- $(authentication)
- $(authentication-type)
- $(authentication-type)
- $(authentication-type)
- ... 还有 89 个命令

### DHCP配置命令 (115个命令)

- $(algorithm)
- $(binding)
- $(check option82)
- $(clear ip dhcp packet statistic)
- $(clear ip dhcp relay dead server)
- $(client option60 format)
- $(client option61 format)
- $(client policy)
- $(client policy)
- $(client start)
- $(client stop)
- $(client write disable)
- $(client write enable)
- $(deadtime)
- $(debug dhcp all)
- ... 还有 100 个命令

### DHCPv6配置命令 (60个命令)

- $(aftr)
- $(algorithm)
- $(binding)
- $(bind-pcp-server)
- $(cos)
- $(deadtime)
- $(debug dhcpv6 all)
- $(debug dhcpv6 filter-by duid)
- $(debug dhcpv6 filter-by interface)
- $(debug dhcpv6 filter-by packet)
- $(debug dhcpv6 filter-by trace)
- $(default server-group)
- $(description)
- $(dhcp ipv6)
- $(dhcpv6-pool)
- ... 还有 45 个命令

### DNS配置命令 (29个命令)

- $(debug dns all)
- $(debug ip domain)
- $(dns instant-refresh)
- $(ip domain deadtime)
- $(ip domain flush-cache)
- $(ip domain hostname)
- $(ip domain lookup)
- $(ip domain max-retry)
- $(ip domain name)
- $(ip domain name-server import interface)
- $(ip domain name-server ipv4-address)
- $(ip domain name-server ipv6-address)
- $(ip domain proxy)
- $(ip domain relay)
- $(ip domain resolve-type)
- ... 还有 14 个命令


