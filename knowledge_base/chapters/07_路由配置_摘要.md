# 07 路由配置

## 章节概述



## 命令数量

- 子章节数：9
- 命令总数：

## 子章节列表

### 路由基础命令 (7个命令)

- $(debug ipv4-route)
- $(ip max-routes)
- $(ip protocol convergence-priority)
- $(ipv6 max-routes)
- $(show debug ipv4-route)
- $(show ip protocol routing capability)
- $(show ip protocol routing)

### BGP配置命令 (1390个命令)

- $(neighbor &lt;mid&gt; split-update-group)
- $(additional-paths different-color)
- $(neighbor &lt;mid&gt; split-update-group)
- $(additional-paths different-color)
- $(neighbor &lt;mid&gt; split-update-group)
- $(show bgp vpnv6 multicast neighbor)
- $(address-family ipv4 sr-policy)
- $(rpki-rtr polling-period)
- $(show ip bgp neighbor vrf)
- $(bgp router-id)
- $(rpki-rtr)
- $(show ip bgp neighbor vrf-in)
- $(bgp route-scan-time)
- $(rpki-validation allow-invalid)
- $(show ip bgp neighbor vrf-in)
- ... 还有 1375 个命令

### ISIS配置命令 (27个命令)

- $(fast-reroute ti-lfa sr-mpls)
- $(ipv6 link-damped bfd-down)
- $(link-damped bfd-down)
- $(metric-auto reference-bandwidth)
- $(metric-auto reference-bandwidth)
- $(router-id)
- $(segment-routing mpls disable)
- $(segment-routing mpls disable)
- $(segment-routing mpls enable)
- $(segment-routing mpls enable)
- $(segment-routing mpls entropy-label disab...)
- $(segment-routing mpls entropy-label disab...)
- $(segment-routing mpls entropy-label enabl...)
- $(segment-routing mpls entropy-label enabl...)
- $(segment-routing srv6 enable)
- ... 还有 12 个命令

### RIP配置命令 (57个命令)

- $(address-family ipv4)
- $(auto-summary)
- $(bfd)
- $(debug ip rip all)
- $(debug ip rip database)
- $(debug ip rip events)
- $(debug ip rip trigger)
- $(debug ip rip)
- $(default-metric)
- $(disable interface)
- $(disable interface)
- $(distance)
- $(enable interface)
- $(enable interface)
- $(error packet rip record)
- ... 还有 42 个命令

### 静态路由配置命令 (2个命令)

- $(ip route nexthop)
- $(ip route)

### VRF命令 (1个命令)

- $(show ip route vpn)

### OSPFv2配置命令 (169个命令)

- $(adjacency-secondary)
- $(advertise interface)
- $(area)
- $(authentication)
- $(authentication)
- $(authentication-key)
- $(authentication-key)
- $(auto-cost)
- $(bfd)
- $(bfd)
- $(bfd)
- $(bfd)
- $(bgp link-state)
- $(capability opaque)
- $(capability vrf-lite)
- ... 还有 154 个命令

### OSPFv3配置命令 (108个命令)

- $(area)
- $(authentication)
- $(auto-cost)
- $(bfd)
- $(bfd)
- $(bfd)
- $(capability vrf-lite)
- $(clear ipv6 ospf process)
- $(cost link-damping)
- $(cost)
- $(dead-interval)
- $(debug ipv6 ospfv3 all)
- $(debug ipv6 ospfv3 events abr)
- $(debug ipv6 ospfv3 events asbr)
- $(debug ipv6 ospfv3 events os)
- ... 还有 93 个命令

### RIPNG配置命令 (29个命令)

- $(clear ipv6 rip)
- $(debug ipv6 ripng all)
- $(debug ipv6 ripng events)
- $(debug ipv6 ripng packet recieve)
- $(debug ipv6 ripng packet send)
- $(default-metric)
- $(distance)
- $(error packet ripng record)
- $(interface)
- $(ipv6 rip enable)
- $(ipv6 rip interface)
- $(ipv6 rip neighbor)
- $(ipv6 rip offset-list)
- $(ipv6 rip originate-default-route)
- $(ipv6 rip poison-reverse)
- ... 还有 14 个命令


